# Chatbot Server

First foundation for a server-side chatbot that will later evolve into a real agent.

## Current features
- FastAPI server
- SQLite message memory
- placeholder local model interface
- `/health` endpoint
- `/chat` endpoint
